#!/bin/bash

# monarch:summary=Switch Monarch branches and update from the selected branch
# monarch:args=<branch>

set -e

if (($# == 0)); then
  echo "Usage: monarch-update-branch [main|dev]"
  exit 1
fi

branch="$1"

exec monarch-update -y --branch "$branch"
