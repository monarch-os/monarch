#!/bin/bash

# monarch:summary=Print the detected touchpad or trackpad device name

device=$(libinput list-devices 2>/dev/null | awk -F': *' '/^Device:/ {name=$2} /^Tap-to-click:/ {if (name ~ /[Tt]ouchpad|[Tt]rackpad/) {print name; exit}}')
[[ -n $device ]] && echo "$device"
