#!/bin/bash

# monarch:summary=Reboot after closing application windows
# monarch:examples=monarch reboot | monarch system reboot
# monarch:aliases=monarch reboot

monarch-state clear re*-required

# Schedule the reboot to happen after closing windows (detached from terminal)
nohup bash -c "sleep 2 && systemctl reboot --no-wall" >/dev/null 2>&1 &

# Now close all windows
monarch-hyprland-window-close-all
sleep 1 # Allow apps like Chrome to shutdown correctly
