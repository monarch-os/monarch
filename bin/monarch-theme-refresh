#!/bin/bash

# monarch:summary=Refresh the current theme from its templates.

THEME_NAME_PATH="$HOME/.config/monarch/current/theme.name"

if [[ -f $THEME_NAME_PATH ]]; then
  monarch-theme-set "$(cat $THEME_NAME_PATH)"
fi
