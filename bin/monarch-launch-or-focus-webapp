#!/bin/bash

# monarch:summary=Launch or focus on a given web app identified by the window-pattern.
# monarch:args=<window-pattern> <url-and-flags...>

if (($# == 0)); then
  echo "Usage: monarch-launch-or-focus-webapp [window-pattern] [url-and-flags...]"
  exit 1
fi

WINDOW_PATTERN="$1"
shift
LAUNCH_COMMAND="monarch-launch-webapp $@"

exec monarch-launch-or-focus "$WINDOW_PATTERN" "$LAUNCH_COMMAND"
