#!/bin/bash

# monarch:summary=Apply the current theme keyboard color to ASUS ROG keyboards
# monarch:hidden=true

ASUSCTL_THEME=~/.config/monarch/current/theme/keyboard.rgb

if monarch-cmd-present asusctl; then
  asusctl aura effect static -c $(sed 's/^#//' $ASUSCTL_THEME)
fi
