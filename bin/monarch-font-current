#!/bin/bash

# monarch:summary=Show current monospace font
# monarch:examples=monarch font current

if [[ -f ~/.config/alacritty/alacritty.toml ]]; then
  grep -oP 'family\s*=\s*"\K[^"]+' ~/.config/alacritty/alacritty.toml | head -n1
fi
