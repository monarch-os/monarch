#!/bin/bash

# monarch:summary=Launch a TUI command in the default terminal with Monarch styling
# monarch:args=<command> [args...]

exec setsid uwsm-app -- xdg-terminal-exec --app-id=org.monarch.$(basename $1) -e "$1" "${@:2}"
