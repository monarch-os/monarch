#!/bin/bash
cd ~/.local/share/monarch
migration_file="$HOME/.local/share/monarch/migrations/$(git log -1 --format=%cd --date=unix).sh"
touch $migration_file

if [[ $1 != "--no-edit" ]]; then
  nvim $migration_file
fi

echo $migration_file
