#!/bin/bash
set -e

# Reinstall the Monarch configuration directory from the git source.

git clone --depth=1 "https://github.com/monarch-os/monarch.git" ~/.local/share/monarch-new >/dev/null
mv $MONARCH_PATH ~/.local/share/monarch-old
mv ~/.local/share/monarch-new $MONARCH_PATH
