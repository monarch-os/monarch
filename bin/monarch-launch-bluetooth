#!/bin/bash

# Launch the Monarch Bluetooth controls TUI (provided by bluetui)
# Also attempts to unblock bluetooth service if rfkill had blocked it.

rfkill unblock bluetooth
exec monarch-launch-or-focus-tui bluetui
