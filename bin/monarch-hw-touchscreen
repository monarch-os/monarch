#!/bin/bash

# monarch:summary=Print the detected touchscreen or tablet device name

device=$(libinput list-devices 2>/dev/null | awk -F': *' '/^Device:/ {name=$2} /^Capabilities:/ {if ($2 ~ /touch|tablet/) {print name; exit}}')
[[ -n $device ]] && echo "$device"
