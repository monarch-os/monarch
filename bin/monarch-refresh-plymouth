#!/bin/bash

# monarch:summary=Overwrite the Plymouth boot theme with the Monarch default and rebuild it.
# monarch:requires-sudo=true

MONARCH_ROOT="${MONARCH_PATH:-/usr/share/monarch}"

exec "$MONARCH_ROOT/bin/monarch-plymouth-apply" --refresh-default
