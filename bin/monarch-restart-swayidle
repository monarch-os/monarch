#!/bin/bash

# monarch:summary=Restart the swayidle daemon (used for idle detection and auto-lock).

pkill -x swayidle 2>/dev/null
setsid uwsm-app -- swayidle -w -C ~/.config/swayidle/config >/dev/null 2>&1 &
