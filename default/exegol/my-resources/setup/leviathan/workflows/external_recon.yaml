name: recon
description: Workflow to emulate reconFTW results
author: y0no
validator: domain

routines:
  - modules:
      - domain_info
      - dns_resolver
      - ip_info
      - zonetransfer
      - subdomain
      - leaks_gathering

    # - emails
    # - google_dorks
    # - github_dorks
    # - metadata
    # - favicon
