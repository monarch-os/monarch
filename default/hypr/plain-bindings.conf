# Application bindings
bindd = SUPER, RETURN, Terminal, exec, uwsm-app -- xdg-terminal-exec --dir="$(monarch-cmd-terminal-cwd)"
bindd = SUPER SHIFT, RETURN, Browser, exec, monarch-launch-browser
bindd = SUPER SHIFT, F, File manager, exec, uwsm-app -- nautilus --new-window
bindd = SUPER ALT SHIFT, F, File manager (cwd), exec, uwsm-app -- nautilus --new-window "$(monarch-cmd-terminal-cwd)"
bindd = SUPER SHIFT, B, Browser, exec, monarch-launch-browser
bindd = SUPER SHIFT ALT, B, Browser (private), exec, monarch-launch-browser --private
bindd = SUPER SHIFT, N, Editor, exec, monarch-launch-editor

# Add extra bindings
# bindd = SUPER SHIFT, A, ChatGPT, exec, monarch-launch-webapp "https://chatgpt.com"
# bindd = SUPER SHIFT, R, exec, alacritty -e ssh your-server

# Overwrite existing bindings, like putting monarch Menu on Super + Space
# unbind = SUPER, SPACE
# bindd = SUPER, SPACE, monarch menu, exec, monarch-menu
