# Deploy and validate the Monarch niri entry-point at ~/.config/niri/config.kdl.
# monarch-settings has already seeded the user-owned includes. Generate the
# canonical entry point and validate the complete include chain.
monarch-refresh-niri
