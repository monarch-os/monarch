#!/bin/bash

set -e

# Remove this hook after use
rm -f "$0"

monarch-notification-send -u critical -t 0 -g "" "Install Dictation with Voxtype" \
  "Click to install voice dictation for Monarch." \
  --action "Install" monarch-launch-floating-terminal-with-presentation monarch-voxtype-install
